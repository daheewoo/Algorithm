//testCase : ()

class Solution {
    public boolean isValid(String s) {
        //String 문자열을 char형 배열로 만들어서 저장하기
        char[] sArray = s.toCharArray();
        
        //using Map to put value
        //indexOf(value?)
        //loop--> delete value
        //after the loop body --> 
        //if Array size equal 1 --> false
        //else(equal 0) --> true
        
        return true;
    }
}
