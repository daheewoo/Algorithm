class Solution {
  public int[] solution(long n) {
      // 배열의 크기를 지정하기위해 String으로 만들어줌
      String nStr = n + "";
      int[] nArr = new int[nStr.length()];
      
      for(int i=0; n>0; i++){
          nArr[i] = (int)(n%10);
          n /=10;

      }
      return nArr;
  }
}
